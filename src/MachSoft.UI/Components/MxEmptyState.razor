<div class="mx-state mx-empty-state @(Compact ? "mx-state-compact" : string.Empty)">
    <MudIcon Icon="@Icons.Material.Outlined.Inbox" />
    <h5>@Title</h5>
    <p>@Description</p>
    @if (Actions is not null)
    {
        <div class="mx-state-actions">@Actions</div>
    }
</div>

@code {
    [Parameter] public string Title { get; set; } = "Sin contenido";
    [Parameter] public string Description { get; set; } = "No hay información para mostrar.";
    [Parameter] public bool Compact { get; set; }
    [Parameter] public RenderFragment? Actions { get; set; }
}
