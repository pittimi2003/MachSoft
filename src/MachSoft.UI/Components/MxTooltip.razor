<MudTooltip Class="mx-tooltip" Text="@Text" Placement="Placement.Bottom" Arrow="true">
    @ChildContent
</MudTooltip>

@code {
    [Parameter] public string Text { get; set; } = string.Empty;
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
